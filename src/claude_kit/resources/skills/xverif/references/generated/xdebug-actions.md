# xdebug 全量 Action 索引

本文件由 `skills/xverif/scripts/generate_references.py` 从 canonical action specs 生成。
用途是保证所有能力可发现；精确参数以 runtime catalog、action-specific schema 和 checked-in example 为准。

| Action | Status | Category | Requires | Purposes | Use when | Do not use when | Alternatives | Required inputs | 中文说明 | English description | Request schema | Example |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `actions` | stable | builtin | none | discover | 需要发现当前 runtime 公开的 action、状态和适用资源。 | 已经知道 action 且需要它的字段、枚举或 response 合同。 | [{"action": "schema", "when": "需要一个已知 action 的完整调用合同。"}] | 以 action schema 为准 | 列出当前运行时公开 action catalog。 | List the public xdebug action catalog. | `xdebug/schemas/v1/actions/actions.request.schema.json` | `xdebug/examples/requests/actions.basic.json` |
| `apb.config.list` | stable | waveform | waveform | discover | Need to discover saved APB interface names before selecting one, without decoding transfers. | Do not use it to register a configuration or inspect APB transfers. | [{"action": "apb.config.load", "when": "Register or replace an APB interface configuration."}, {"action": "apb.query", "when": "Inspect decoded APB transfers."}] | 以 action schema 为准 | 列出 APB 配置。 | List saved APB interface configurations. | `xdebug/schemas/v1/actions/apb.config.list.request.schema.json` | `xdebug/examples/requests/apb.config.list.basic.json` |
| `apb.config.load` | stable | waveform | waveform | configure | Have an inline APB signal mapping or config file that must be registered and validated before querying transfers. | Do not use it to enumerate saved names or query decoded APB activity. | [{"action": "apb.config.list", "when": "Discover saved APB configuration names."}, {"action": "apb.query", "when": "Inspect transfers from a loaded interface."}] | name, one of config, one of config_path | 加载 APB 配置。 | Load and validate an APB interface configuration. | `xdebug/schemas/v1/actions/apb.config.load.request.schema.json` | `xdebug/examples/requests/apb.config.load.basic.json` |
| `apb.transaction.cursor` | stable | waveform | waveform | query | Need stateful begin/next/prev navigation through decoded APB transfers one item at a time. | Do not use it for arbitrary waveform time bookmarks or bulk APB filtering. | [{"action": "waveform.cursor.set", "when": "Store an arbitrary waveform time bookmark."}, {"action": "apb.query", "when": "Filter or enumerate APB transfers."}] | name, op | 在 APB transfer 间移动游标。 | Move a cursor across APB transfers. | `xdebug/schemas/v1/actions/apb.transaction.cursor.request.schema.json` | `xdebug/examples/requests/apb.transaction.cursor.basic.json` |
| `apb.export` | stable | waveform | waveform | export | Need a bounded in-response APB preview or one complete TSV/CSV transaction artifact plus metadata. | Do not use it for aggregate-only APB counts, interactive transaction selection, AXI semantics, or generic valid-ready streams. | [{"action": "apb.query", "when": "Interactively select or inspect APB transactions without creating an artifact."}, {"action": "apb.statistics", "when": "Need only aggregate APB counts and unresolved-filter diagnostics."}] | name, time_range | 预览或导出完整时间范围内过滤后的 APB 事务。 | Preview or export filtered APB transactions from one complete time range. | `xdebug/schemas/v1/actions/apb.export.request.schema.json` | `xdebug/examples/requests/apb.export.basic.json` |
| `apb.query` | stable | waveform | waveform | query | Need completed APB transfer counts or rows filtered by direction and a canonical exact/range/mask address object. | Do not use it for AXI channel semantics, custom valid-ready fields, or a focused signal window around one APB transfer. | [{"action": "apb.export", "when": "Persist the complete filtered APB transaction set as TSV or CSV."}, {"action": "apb.statistics", "when": "Need aggregate APB counts and unresolved-filter diagnostics without transaction rows."}, {"action": "apb.transfer_window", "when": "Inspect signal evidence around one APB transfer."}] | name | 按方向以及 exact、range 或 mask 地址条件查询已完成 APB transfer。 | Query completed APB transfers with direction and exact, range, or mask address filters. | `xdebug/schemas/v1/actions/apb.query.request.schema.json` | `xdebug/examples/requests/apb.query.basic.json` |
| `apb.transfer_window` | experimental | waveform | waveform | analyze | Have one APB transfer index and need the surrounding raw-signal timing evidence. | Do not use it to enumerate many APB transfers or return aggregate protocol counts. | [{"action": "apb.query", "when": "Select APB transfers by direction, address, or index."}, {"action": "apb.statistics", "when": "Return aggregate APB counts."}] | name | 实验性 APB 窗口分析。 | Inspect the waveform window around an APB transfer. | `xdebug/schemas/v1/actions/apb.transfer_window.request.schema.json` | `xdebug/examples/requests/apb.transfer_window.basic.json` |
| `apb.statistics` | stable | waveform | waveform | analyze | Count completed APB read and write transactions after applying protocol filters. | Inspecting individual APB transaction payloads or pending transfers. | [{"action": "apb.query", "when": "Inspect individual filtered APB transactions and payloads."}, {"action": "apb.export", "when": "Persist the complete filtered APB transaction set as TSV or CSV."}, {"action": "apb.transfer_window", "when": "Inspect raw signal evidence around one selected APB transfer."}] | name | 按方向和地址过滤统计已完成 APB 事务。 | Count completed APB transactions with optional direction and address filters. | `xdebug/schemas/v1/actions/apb.statistics.request.schema.json` | `xdebug/examples/requests/apb.statistics.basic.json` |
| `axi.analysis` | stable | waveform | waveform | analyze | Need a derived latency, pending, or outstanding analysis across reconstructed AXI transactions rather than raw rows. | Do not use it for a raw transaction listing, a single outlier ranking, or aggregate filtered counts. | [{"action": "axi.query", "when": "Inspect selected AXI channels or reconstructed transactions."}, {"action": "axi.latency_outlier", "when": "Rank or threshold abnormal-latency transactions."}, {"action": "axi.statistics", "when": "Count completed AXI transactions after filters."}] | name | 汇总 AXI 行为。 | Analyze AXI latency, pending transactions, and outstanding activity. | `xdebug/schemas/v1/actions/axi.analysis.request.schema.json` | `xdebug/examples/requests/axi.analysis.basic.json` |
| `axi.export` | stable | waveform | waveform | export | Need selected AXI transaction evidence persisted to the declared artifact path and file format. | Do not use it when the result should remain only in the response or when no artifact is required. | [{"action": "axi.query", "when": "Inspect AXI evidence in the response without creating an artifact."}] | name, one of time_range | 导出 AXI 数据。 | Export AXI transactions for external analysis. | `xdebug/schemas/v1/actions/axi.export.request.schema.json` | `xdebug/examples/requests/axi.export.basic.json` |
| `axi.channel_stall` | experimental | waveform | waveform | query | Need intervals and counts where one AXI channel keeps VALID asserted while READY is deasserted. | Do not use it for generic non-AXI valid-ready interfaces or whole-transaction latency analysis. | [{"action": "protocol.handshake.inspect", "when": "Inspect a generic valid-ready handshake."}, {"action": "axi.latency_outlier", "when": "Find end-to-end AXI transaction latency outliers."}] | name | 实验性 AXI stall 分析。 | Inspect AXI channel stall windows. | `xdebug/schemas/v1/actions/axi.channel_stall.request.schema.json` | `xdebug/examples/requests/axi.channel_stall.basic.json` |
| `axi.config.list` | stable | waveform | waveform | discover | Need to discover saved AXI interface names before selecting one, without decoding traffic. | Do not use it to register a configuration or inspect AXI traffic. | [{"action": "axi.config.load", "when": "Register or replace an AXI interface configuration."}, {"action": "axi.query", "when": "Inspect decoded AXI traffic."}] | 以 action schema 为准 | 列出 AXI 配置。 | List saved AXI interface configurations. | `xdebug/schemas/v1/actions/axi.config.list.request.schema.json` | `xdebug/examples/requests/axi.config.list.basic.json` |
| `axi.config.load` | stable | waveform | waveform | configure | Have an inline AXI channel mapping or config file that must be registered and validated before traffic analysis. | Do not use it to enumerate saved names or query decoded AXI activity. | [{"action": "axi.config.list", "when": "Discover saved AXI configuration names."}, {"action": "axi.query", "when": "Inspect traffic from a loaded interface."}] | name, one of config, one of config_path | 加载 AXI 配置。 | Load and validate an AXI interface configuration. | `xdebug/schemas/v1/actions/axi.config.load.request.schema.json` | `xdebug/examples/requests/axi.config.load.basic.json` |
| `axi.transaction.cursor` | stable | waveform | waveform | query | Need stateful begin/next/prev navigation through reconstructed AXI transactions one item at a time. | Do not use it for arbitrary waveform time bookmarks or bulk AXI filtering. | [{"action": "waveform.cursor.set", "when": "Store an arbitrary waveform time bookmark."}, {"action": "axi.query", "when": "Filter or enumerate AXI transactions."}] | name, op | 在 AXI transfer 间移动游标。 | Move a cursor across AXI transactions. | `xdebug/schemas/v1/actions/axi.transaction.cursor.request.schema.json` | `xdebug/examples/requests/axi.transaction.cursor.basic.json` |
| `axi.latency_outlier` | experimental | waveform | waveform | analyze | Need to rank or threshold reconstructed AXI transactions by end-to-end latency. | Do not use it for the complete transaction list or outstanding-depth evolution. | [{"action": "axi.query", "when": "Inspect the complete selected transaction evidence."}, {"action": "axi.outstanding_timeline", "when": "Inspect outstanding-depth changes over time."}] | name | 实验性 AXI latency 异常。 | Find AXI transactions with abnormal latency. | `xdebug/schemas/v1/actions/axi.latency_outlier.request.schema.json` | `xdebug/examples/requests/axi.latency_outlier.basic.json` |
| `axi.outstanding_timeline` | experimental | waveform | waveform | analyze | Need the time-ordered evolution of AXI pending or outstanding depth, including peak intervals. | Do not use it to inspect payload beats or rank individual transaction latency. | [{"action": "axi.query", "when": "Inspect transaction or channel payload evidence."}, {"action": "axi.latency_outlier", "when": "Find individual latency outliers."}] | name | 实验性 AXI outstanding 时间线。 | Analyze the AXI pending-transaction timeline. | `xdebug/schemas/v1/actions/axi.outstanding_timeline.request.schema.json` | `xdebug/examples/requests/axi.outstanding_timeline.basic.json` |
| `axi.query` | stable | waveform | waveform | query | Need reconstructed AXI transaction evidence, first-beat data, per-channel handshake times, or a precise channel-handshake lookup. | Do not use it for custom valid-ready fields, artifact generation, or latency/outstanding aggregation. | [{"action": "axi.statistics", "when": "Return filtered AXI counts."}, {"action": "axi.export", "when": "Write AXI transaction artifacts."}, {"action": "axi.analysis", "when": "Run latency, pending, or outstanding analysis."}] | name | 按通道握手，或按方向、地址、ID 与地址握手时间查询重建后的 AXI transaction。 | Query reconstructed AXI transactions by channel handshake or direction, address, ID, and address-handshake time filters. | `xdebug/schemas/v1/actions/axi.query.request.schema.json` | `xdebug/examples/requests/axi.query.basic.json` |
| `axi.request_response_pair` | experimental | waveform | waveform | analyze | Need explicit request-to-response correlation and pairing evidence for reconstructed AXI transactions. | Do not use it for unrelated channel handshakes or an unpaired transaction listing. | [{"action": "axi.query", "when": "Inspect arbitrary AXI channels or transactions."}, {"action": "axi.analysis", "when": "Analyze protocol-wide pending or latency behavior."}] | name | 实验性 AXI 请求响应配对。 | Pair AXI requests with their responses. | `xdebug/schemas/v1/actions/axi.request_response_pair.request.schema.json` | `xdebug/examples/requests/axi.request_response_pair.basic.json` |
| `axi.statistics` | stable | waveform | waveform | analyze | Count completed AXI read and write transactions after applying protocol filters. | Inspecting individual AXI payload beats or pending transactions. | [{"action": "axi.query", "when": "Inspect filtered AXI transactions, channels, or payload beats."}, {"action": "axi.analysis", "when": "Analyze AXI pending transactions, latency, or outstanding activity."}, {"action": "axi.export", "when": "Persist selected AXI transaction evidence as an artifact."}] | name | 按方向、ID 和地址过滤统计已完成 AXI 事务。 | Count completed AXI transactions with optional direction, ID, and address filters. | `xdebug/schemas/v1/actions/axi.statistics.request.schema.json` | `xdebug/examples/requests/axi.statistics.basic.json` |
| `batch` | stable | builtin | none | orchestrate | 需要按顺序执行多个相互独立的 native xdebug request。 | 需要一个 MCP session 中的单个 action 查询或跨请求共享结果。 | [{"action": "schema", "when": "需要先验证单个 request 合同。"}] | requests | 批量执行多个 xdebug request。 | Execute multiple xdebug requests as one ordered batch. | `xdebug/schemas/v1/actions/batch.request.schema.json` | `xdebug/examples/requests/batch.basic.json` |
| `counter.statistics` | stable | waveform | waveform | analyze | 需要按采样 clock 统计一个 counter 的增量、回绕或活动。 | 需要多个一般信号的活动统计或原始变化列表。 | [{"action": "signal.statistics", "when": "需要一般信号而非 counter 语义的统计。"}] | clock, time_range, vld, cnt | 统计计数器行为。 | Analyze sampled counter behavior and statistics. | `xdebug/schemas/v1/actions/counter.statistics.request.schema.json` | `xdebug/examples/requests/counter.statistics.basic.json` |
| `waveform.cursor.delete` | stable | waveform | waveform | manage | Know the waveform cursor name and need to remove that saved time bookmark. | Do not use it to delete protocol transaction state or to move a cursor. | [{"action": "waveform.cursor.list", "when": "Discover waveform cursor names before deletion."}, {"action": "waveform.cursor.set", "when": "Create or replace a waveform cursor."}] | name | 删除游标。 | Delete a named waveform cursor. | `xdebug/schemas/v1/actions/waveform.cursor.delete.request.schema.json` | `xdebug/examples/requests/waveform.cursor.delete.basic.json` |
| `waveform.cursor.get` | stable | waveform | waveform | query, manage | Know the waveform cursor name and need its stored time without changing the active cursor. | Do not use it to activate a cursor or navigate APB/AXI transactions. | [{"action": "waveform.cursor.use", "when": "Activate a saved waveform cursor."}, {"action": "apb.transaction.cursor", "when": "Navigate APB transactions."}, {"action": "axi.transaction.cursor", "when": "Navigate AXI transactions."}] | name | 读取命名游标。 | Read a named waveform cursor. | `xdebug/schemas/v1/actions/waveform.cursor.get.request.schema.json` | `xdebug/examples/requests/waveform.cursor.get.basic.json` |
| `waveform.cursor.list` | stable | waveform | waveform | discover, manage | Need to discover available waveform time-bookmark names before reading or activating one. | Do not use it to read one cursor time or enumerate protocol transactions. | [{"action": "waveform.cursor.get", "when": "Read one saved waveform cursor."}, {"action": "apb.query", "when": "Enumerate APB transactions."}, {"action": "axi.query", "when": "Enumerate AXI transactions."}] | 以 action schema 为准 | 列出游标。 | List named waveform cursors. | `xdebug/schemas/v1/actions/waveform.cursor.list.request.schema.json` | `xdebug/examples/requests/waveform.cursor.list.basic.json` |
| `waveform.cursor.set` | stable | waveform | waveform | configure, manage | Need to create or replace an arbitrary waveform time bookmark under a stable name. | Do not use it to navigate decoded protocol transactions. | [{"action": "apb.transaction.cursor", "when": "Move across APB transfers."}, {"action": "axi.transaction.cursor", "when": "Move across AXI transactions."}] | name, time | 保存命名时间游标。 | Save a named waveform cursor. | `xdebug/schemas/v1/actions/waveform.cursor.set.request.schema.json` | `xdebug/examples/requests/waveform.cursor.set.basic.json` |
| `waveform.cursor.use` | stable | waveform | waveform | configure, manage | Need to make one existing waveform time bookmark the active cursor without changing its saved time. | Do not use it to create a cursor or navigate decoded protocol transactions. | [{"action": "waveform.cursor.set", "when": "Create or replace a waveform cursor."}, {"action": "apb.transaction.cursor", "when": "Navigate APB transfers."}, {"action": "axi.transaction.cursor", "when": "Navigate AXI transactions."}] | name | 激活游标。 | Activate a named waveform cursor. | `xdebug/schemas/v1/actions/waveform.cursor.use.request.schema.json` | `xdebug/examples/requests/waveform.cursor.use.basic.json` |
| `signal.anomaly.inspect` | stable | waveform | waveform | inspect | 需要在 raw waveform 中检查 X/Z、短脉冲或长时间不变。 | 需要证明 valid-ready 协议违规。 | [{"action": "protocol.handshake.inspect", "when": "需要协议层 stall 或稳定性结论。"}] | signals | 扫描常见波形异常。 | Detect waveform glitches, unknown values, and stuck signals. | `xdebug/schemas/v1/actions/signal.anomaly.inspect.request.schema.json` | `xdebug/examples/requests/signal.anomaly.inspect.basic.json` |
| `event.config.list` | stable | waveform | waveform | discover | Need to enumerate or inspect saved event-query definitions without executing a waveform search. | Do not use it to load a named event configuration or search waveform events. | [{"action": "event.config.load", "when": "Load one event-query configuration."}, {"action": "event.find", "when": "Search sampled waveform events."}] | 以 action schema 为准 | 列出事件配置。 | List saved event-query configurations. | `xdebug/schemas/v1/actions/event.config.list.request.schema.json` | `xdebug/examples/requests/event.config.list.basic.json` |
| `event.config.load` | stable | waveform | waveform | configure | Have a persisted event-query configuration that must be read and registered before event search or export. | Do not use it to enumerate configuration names or execute the configured event search. | [{"action": "event.config.list", "when": "Discover saved event configuration names."}, {"action": "event.find", "when": "Execute an event search."}] | name, config_path | 加载事件查询配置。 | Load an event-query configuration. | `xdebug/schemas/v1/actions/event.config.load.request.schema.json` | `xdebug/examples/requests/event.config.load.basic.json` |
| `event.export` | stable | waveform | waveform | export | Need all matching sampled-event evidence written to a declared artifact rather than returned only inline. | Do not use it when no artifact is required or when only the first/last matching sample is needed. | [{"action": "event.find", "when": "Return matching sampled events in the response."}] | expr, one of name, one of clock/signals | 导出满足表达式的事件。 | Export sampled events that match an expression. | `xdebug/schemas/v1/actions/event.export.request.schema.json` | `xdebug/examples/requests/event.export.basic.json` |
| `event.find` | stable | waveform | waveform | query | 需要按 clock edge 查找满足表达式的采样事件。 | 需要原始 value-change timeline 或标准 AXI/APB transaction。 | [{"action": "signal.changes", "when": "需要原始跳变。"}] | expr, one of name, one of clock/signals | 查找满足表达式的事件样例。 | Find sampled events that match an expression. | `xdebug/schemas/v1/actions/event.find.request.schema.json` | `xdebug/examples/requests/event.find.basic.json` |
| `expr.eval_at` | stable | waveform | waveform | transform | Have a waveform-backed Boolean or value expression and need its sampled result at one canonical time. | Do not use it to rewrite expression syntax or inspect assignment structure without sampling. | [{"action": "expr.normalize", "when": "Normalize expression text or a design assignment without waveform evaluation."}, {"action": "value.at", "when": "Read one raw leaf signal value."}] | expr, time, signals, clock | 在指定时间求布尔/表达式值。 | Evaluate an expression at a sampled waveform time. | `xdebug/schemas/v1/actions/expr.eval_at.request.schema.json` | `xdebug/examples/requests/expr.eval_at.basic.json` |
| `expr.normalize` | stable | design | conditional: expression=none; design_signal=design | transform | Need a deterministic parsed expression AST, or a design assignment normalized from one exact signal, without waveform sampling. | Do not use it to evaluate an expression at waveform time or to resolve a signal name to candidates. | [{"action": "expr.eval_at", "when": "Evaluate an expression at a sampled waveform time."}, {"action": "signal.resolve", "when": "Find matching design signal objects."}] | expression (requires none; required expr; forbids signal/line_limit/no_statement_only/role); design_signal (requires design; required signal; forbids expr) | 规范化表达式。 | Normalize an expression for deterministic processing. | `xdebug/schemas/v1/actions/expr.normalize.request.schema.json` | `xdebug/examples/requests/expr.normalize.basic.json` |
| `protocol.handshake.inspect` | stable | waveform | waveform | inspect | 需要检查通用 valid-ready transfer、stall 或数据稳定性。 | 需要 AXI/APB 专用 transaction 关联。 | [{"action": "axi.query", "when": "接口是标准 AXI。"}] | clock, valid, ready | 检查 valid/ready 握手。 | Inspect valid-ready transfers, stalls, and stability violations. | `xdebug/schemas/v1/actions/protocol.handshake.inspect.request.schema.json` | `xdebug/examples/requests/protocol.handshake.inspect.basic.json` |
| `list.add` | stable | waveform | waveform | configure, manage | Have an existing named waveform list and need to append one specific signal path. | Do not use it to create the list, delete entries, or inspect waveform values. | [{"action": "list.create", "when": "Create the named list first."}, {"action": "list.delete", "when": "Remove an entry."}, {"action": "value.at", "when": "Sample all list signals at one or more times."}] | name, signal | 向信号列表追加一个信号。 | Add a signal to a named waveform list. | `xdebug/schemas/v1/actions/list.add.request.schema.json` | `xdebug/examples/requests/list.add.basic.json` |
| `list.create` | stable | waveform | waveform | configure, manage | Need a new or replaced named collection of waveform signal paths for repeated multi-signal operations. | Do not use it to append to an existing list or load protocol/stream configuration. | [{"action": "list.add", "when": "Append one signal to an existing list."}, {"action": "stream.config.load", "when": "Load a valid-ready stream definition."}] | name | 创建命名信号列表。 | Create a named waveform signal list. | `xdebug/schemas/v1/actions/list.create.request.schema.json` | `xdebug/examples/requests/list.create.basic.json` |
| `list.load` | stable | waveform | waveform | configure, manage | Have a schema-valid inline or file-backed JSON configuration for key signals that must be loaded atomically before repeated multi-signal inspection. | Do not use it for one ad-hoc list that can be created directly with list.create, or for protocol interface configuration. | [{"action": "list.create", "when": "Create one temporary named list directly from a signals array."}, {"action": "stream.config.load", "when": "Load a valid-ready stream definition with sampling semantics."}] | one of config, one of config_path | 从 JSON 配置加载并验证一个或多个命名波形信号列表。 | Load and validate one or more named waveform signal lists from JSON configuration. | `xdebug/schemas/v1/actions/list.load.request.schema.json` | `xdebug/examples/requests/list.load.basic.json` |
| `list.delete` | stable | waveform | waveform | configure, manage | Know a stored signal path or one-based list index and need to remove only that entry. | Do not use it to remove the whole list or inspect list contents. | [{"action": "list.show", "when": "Inspect current one-based indexes and signal paths."}, {"action": "list.create", "when": "Replace the named list with a new definition."}] | name, one of signal, one of index | 从信号列表删除信号或 index。 | Delete a signal or index from a waveform list. | `xdebug/schemas/v1/actions/list.delete.request.schema.json` | `xdebug/examples/requests/list.delete.basic.json` |
| `list.first_change` | stable | waveform | waveform | analyze | Need the earliest time any member of a saved waveform list diverges within a closed interval. | Do not use it for a full change timeline or point sampling. | [{"action": "signal.changes", "when": "Return every raw change of one signal."}, {"action": "value.at", "when": "Sample all list signals at one or more times."}] | name, time_range | 查找 list 在时间窗口内的首次差异。 | Find the first difference in a waveform list over a time range. | `xdebug/schemas/v1/actions/list.first_change.request.schema.json` | `xdebug/examples/requests/list.first_change.basic.json` |
| `list.export` | stable | waveform | waveform | export | Need multi-signal values from a saved list written to a declared artifact or returned as an export preview. | Do not use it when no file or multi-signal preview is needed, or for a single point read. | [{"action": "value.at", "when": "Read list values at one or more times."}, {"action": "list.load", "when": "Load a reusable signal set from JSON configuration before point reads."}] | name | 导出 list 数据。 | Export waveform-list data. | `xdebug/schemas/v1/actions/list.export.request.schema.json` | `xdebug/examples/requests/list.export.basic.json` |
| `list.show` | stable | waveform | waveform | query | Know the list name and need its stored one-based entries without waveform validation or sampling. | Do not use it to validate design existence or sample waveform values. | [{"action": "list.validate", "when": "Check every stored path against the active waveform."}, {"action": "value.at", "when": "Sample the list at one or more times."}] | name | 显示信号列表内容。 | Show the contents of a waveform signal list. | `xdebug/schemas/v1/actions/list.show.request.schema.json` | `xdebug/examples/requests/list.show.basic.json` |
| `list.validate` | stable | waveform | waveform | verify | Need to prove every stored list entry resolves against the active waveform before sampling or export. | Do not use it merely to display stored names or to evaluate their waveform values. | [{"action": "list.show", "when": "Display stored list contents."}, {"action": "value.at", "when": "Read waveform values after validation."}] | name | 验证 list 中信号是否存在。 | Validate signal paths stored in a waveform list. | `xdebug/schemas/v1/actions/list.validate.request.schema.json` | `xdebug/examples/requests/list.validate.basic.json` |
| `signal.sampled_pulse.inspect` | experimental | waveform | waveform | inspect | 需要发现未被指定 clock sample 捕获的 valid 短脉冲。 | 需要 raw glitch/stuck 扫描或 valid-ready 协议违规结论。 | [{"action": "signal.anomaly.inspect", "when": "需要 raw pulse/glitch/stuck 异常。"}, {"action": "protocol.handshake.inspect", "when": "需要 valid-ready 协议检查。"}] | clock, valid | 检查未被 clock 采到的 valid 短脉冲。 | Inspect valid pulses that were not observed at sampling edges. | `xdebug/schemas/v1/actions/signal.sampled_pulse.inspect.request.schema.json` | `xdebug/examples/requests/signal.sampled_pulse.inspect.basic.json` |
| `schema` | stable | builtin | none | discover | 已知 action 名称，需要其 request 或 response 合同。 | 尚未知道 action 名称，或需要实际执行一个 debug 查询。 | [{"action": "actions", "when": "需要先发现 action catalog。"}] | action | 返回指定 action 的 request/response JSON schema。 | Return the request or response schema for an action. | `xdebug/schemas/v1/actions/schema.request.schema.json` | `xdebug/examples/requests/schema.basic.json` |
| `scope.list` | stable | waveform | any | discover | Need bounded hierarchy discovery from waveform, design, or both, including generate scopes, interfaces, interface arrays, modports, and mpports. | Do not use it when only database roots are needed or one known leaf signal must be resolved. | [{"action": "scope.roots", "when": "Return only waveform/design roots."}, {"action": "signal.resolve", "when": "Resolve one design signal query."}] | 以 action schema 为准 | 有界列出 waveform、design 或 merged 层级对象，并保留来源与可查询/可追踪能力证据。 | List bounded waveform, design, or merged hierarchy objects with source and capability evidence. | `xdebug/schemas/v1/actions/scope.list.request.schema.json` | `xdebug/examples/requests/scope.list.basic.json` |
| `scope.roots` | stable | waveform | any | discover | Need only the database root scopes as entry points before choosing a narrower hierarchy query. | Do not use it for module hierarchy contents or module/port/signal filtering. | [{"action": "scope.list", "when": "List module-relative modules, ports, signals, and interface instances with level and glob filters."}] | 以 action schema 为准 | 发现 waveform/design 根。 | Discover waveform and design hierarchy roots. | `xdebug/schemas/v1/actions/scope.roots.request.schema.json` | `xdebug/examples/requests/scope.roots.basic.json` |
| `session.close` | stable | session | session | manage | Use the default graceful mode for a healthy session, or explicit force mode for stale or unhealthy state that cannot complete graceful shutdown.; Use target.session_id=all to close all registered sessions and receive per-session retained failure details. | Do not use force mode merely to inspect session health.; Do not provide args.ownership_token except for force cleanup of one exact session id; it is a managed-wrapper match precondition, not authorization, and is forbidden with target.session_id=all. | [{"action": "session.doctor", "when": "Diagnose the session without changing it."}] | 以 action schema 为准 | 以 graceful 或 force 模式关闭一个或全部 session。 | Close one or all debug sessions gracefully or forcibly. | `xdebug/schemas/v1/actions/session.close.request.schema.json` | `xdebug/examples/requests/session.close.basic.json` |
| `session.doctor` | stable | session | session | manage | Know a session identifier and need read-only health, resource, process, and accessibility diagnostics. | Do not use it to open, close, or garbage-collect sessions. | [{"action": "session.list", "when": "Discover session identifiers."}, {"action": "session.close", "when": "Gracefully close one session."}] | 以 action schema 为准 | 诊断当前 session。 | Diagnose debug-session resources and accessibility. | `xdebug/schemas/v1/actions/session.doctor.request.schema.json` | `xdebug/examples/requests/session.doctor.basic.json` |
| `session.gc` | stable | session | none | manage | Need policy-driven cleanup of all expired session records rather than mutation of one named active session. | Do not use it to remove a specific active session or diagnose one session. | [{"action": "session.close", "when": "Close one explicit session with args.mode=force."}, {"action": "session.doctor", "when": "Diagnose one explicit session."}] | 以 action schema 为准 | 清理过期 session。 | Garbage-collect expired debug sessions. | `xdebug/schemas/v1/actions/session.gc.request.schema.json` | `xdebug/examples/requests/session.gc.basic.json` |
| `session.list` | stable | session | none | discover, manage | Need to discover session identifiers and lifecycle states without probing resources or mutating them. | Do not use it to diagnose resources or mutate session lifecycle. | [{"action": "session.doctor", "when": "Inspect one session's health and resources."}, {"action": "session.open", "when": "Create a new session."}] | 以 action schema 为准 | 列出当前 session。 | List active debug sessions. | `xdebug/schemas/v1/actions/session.list.request.schema.json` | `xdebug/examples/requests/session.list.basic.json` |
| `session.open` | stable | session | any | manage | Need a new managed session bound to explicit design, waveform, or combined resources. | Do not use it when an existing session already owns the required resources or when only discovery is needed.; Do not treat public args.ownership_token as authorization; ordinary CLI callers omit it, while the frontend still binds a fail-closed internally generated cleanup token before every backend open unless a managed wrapper supplied its own. The key must never be exposed or logged. | [{"action": "session.list", "when": "Discover existing sessions."}, {"action": "session.doctor", "when": "Verify an existing session's health."}] | name | 打开 design/waveform session。 | Open a design, waveform, or combined debug session. | `xdebug/schemas/v1/actions/session.open.request.schema.json` | `xdebug/examples/requests/session.open.basic.json` |
| `signal.canonicalize` | stable | design | design | transform | Need one unique exact design match followed through static port connectivity to a canonical path. | Do not use it when multiple candidate discovery is desired or when waveform values are required. | [{"action": "signal.resolve", "when": "Return design candidates without selecting a canonical connectivity path."}, {"action": "value.at", "when": "Read the waveform value of a known leaf path."}] | signal | 返回信号 canonical 名称。 | Return the canonical design path for a signal. | `xdebug/schemas/v1/actions/signal.canonicalize.request.schema.json` | `xdebug/examples/requests/signal.canonicalize.basic.json` |
| `signal.changes` | stable | waveform | waveform | query | 需要一个信号的原始波形变化时间线，或通过 mode=summary 读取不含逐项变化的聚合事实。 | 需要按 clock edge 的协议语义采样。 | [{"action": "event.find", "when": "需要按 clock 对表达式采样。"}] | signal | 读取信号变化点。 | List raw waveform value changes for a signal. | `xdebug/schemas/v1/actions/signal.changes.request.schema.json` | `xdebug/examples/requests/signal.changes.basic.json` |
| `signal.resolve` | stable | design | design | transform | Need the complete candidate set for a design signal query and do not want implicit selection. | Do not use it to follow static port connectivity to one canonical path or to sample waveform values. | [{"action": "signal.canonicalize", "when": "Require one unambiguous canonical connectivity result."}, {"action": "value.at", "when": "Read a known waveform path."}] | signal | 解析设计信号。 | Resolve a design signal to canonical candidates. | `xdebug/schemas/v1/actions/signal.resolve.request.schema.json` | `xdebug/examples/requests/signal.resolve.basic.json` |
| `signal.stability` | stable | waveform | waveform | analyze | Need a yes/no stability verdict and violating intervals for one signal over a closed time range. | Do not use it for a complete raw-change timeline or aggregate activity statistics. | [{"action": "signal.changes", "when": "Return raw value changes."}, {"action": "signal.statistics", "when": "Compute sampled activity statistics."}] | signal | 检查信号窗口内是否稳定。 | Check whether a signal stays stable in a time range. | `xdebug/schemas/v1/actions/signal.stability.request.schema.json` | `xdebug/examples/requests/signal.stability.basic.json` |
| `signal.statistics` | stable | waveform | waveform | analyze | Need aggregate sampled activity counts or value distributions for one signal under a clock/condition contract. | Do not use it for a raw change-by-change timeline or a yes/no stability verdict. | [{"action": "signal.changes", "when": "Return every raw change."}, {"action": "signal.stability", "when": "Check whether a signal remains stable."}] | signal | 统计信号活动。 | Measure waveform signal activity and sampled values. | `xdebug/schemas/v1/actions/signal.statistics.request.schema.json` | `xdebug/examples/requests/signal.statistics.basic.json` |
| `signal.xz_verify` | experimental | waveform | waveform | verify | 需要证明 raw waveform 在整个闭区间内始终为指定 X/Z 状态。 | 只需要寻找一次 X/Z，或需要 clock-edge 表达式验证。 | [{"action": "signal.anomaly.inspect", "when": "只需要发现窗口内是否曾出现 X/Z。"}, {"action": "window.verify", "when": "需要按 clock edge 验证已知值表达式。"}] | signal, expected_state, time_range | 检查信号在闭合时间窗口内是否始终满足指定的 X 或 Z 状态。 | Verify that every raw waveform value in a closed time window matches an X or Z state. | `xdebug/schemas/v1/actions/signal.xz_verify.request.schema.json` | `xdebug/examples/requests/signal.xz_verify.basic.json` |
| `trace.active_driver` | stable | combined | combined | trace | Need to identify which static driver is actually controlling a signal at one waveform time, with dynamic evidence. | Do not use it for static driver enumeration or recursive root-cause traversal. | [{"action": "trace.driver", "when": "Enumerate static design drivers."}, {"action": "trace.active_driver_chain", "when": "Recursively follow one active-driver chain."}, {"action": "trace.x_origin", "when": "Trace multiple X-producing branches."}] | signal, time | 在指定时间找 active driver。 | Find active driver evidence at a specific waveform time. | `xdebug/schemas/v1/actions/trace.active_driver.request.schema.json` | `xdebug/examples/requests/trace.active_driver.basic.json` |
| `trace.active_driver_chain` | stable | combined | combined | trace | 需要从指定 signal/time 递归展开单条 active-driver chain；limits.max_depth 默认 8。 | 查询点是 X，且需要同时追踪多个 X RHS/control 分支。 | [{"action": "trace.x_origin", "when": "需要按 X 可见性进行多分支追踪。"}] | signal, time | 展开 active driver 链。 | Trace an active-driver chain across multiple levels. | `xdebug/schemas/v1/actions/trace.active_driver_chain.request.schema.json` | `xdebug/examples/requests/trace.active_driver_chain.basic.json` |
| `trace.x_origin` | experimental | combined | combined | trace | 查询点含 X，需要按 DFS 同时追踪 RHS、control、端口和时间边界；纯 port/interface/modport/ref alias 路径会归并，响应会区分 query_time、x_onset_time 与 active_time。 | 只需要静态 driver 枚举，或查询点不含 X。 | [{"action": "trace.active_driver", "when": "只需要指定时刻的单级 active driver。"}, {"action": "trace.active_driver_chain", "when": "需要传统单链 active-driver 展开。"}] | signal, time | 从指定信号和时间按有效语义 DFS 多分支追溯 X 的产生位置或不可见边界。 | Trace an X value backward through effective semantic DFS branches to its sources or observability boundaries. | `xdebug/schemas/v1/actions/trace.x_origin.request.schema.json` | `xdebug/examples/requests/trace.x_origin.basic.json` |
| `trace.driver` | stable | design | design | trace | Need all structural driver pins or assignments for a design signal without evaluating waveform activity. | Do not use it to decide which driver is active at a waveform time. | [{"action": "trace.active_driver", "when": "Find active driver evidence at one time."}, {"action": "trace.load", "when": "Enumerate static consumers instead of drivers."}] | signal | 查找信号直接 driver。 | Find static drivers of a design signal. | `xdebug/schemas/v1/actions/trace.driver.request.schema.json` | `xdebug/examples/requests/trace.driver.basic.json` |
| `trace.load` | stable | design | design | trace | Need all structural consumer pins and fanout endpoints for a design signal without waveform evaluation. | Do not use it to enumerate drivers or decide waveform-time activity. | [{"action": "trace.driver", "when": "Enumerate static drivers."}, {"action": "trace.active_driver", "when": "Find the active driver at one time."}] | signal | 查找信号 load。 | Find static loads of a design signal. | `xdebug/schemas/v1/actions/trace.load.request.schema.json` | `xdebug/examples/requests/trace.load.basic.json` |
| `nwave.rc.generate` | stable | waveform | waveform | export | 需要从分组信号生成可复用 waveform rc 配置。 | 需要直接导出当前 waveform evidence 或查询一个信号。 | [{"action": "list.export", "when": "需要导出 list 的当前数据。"}] | config_path, output | 根据分组生成波形 rc。 | Generate a grouped waveform-view RC file. | `xdebug/schemas/v1/actions/nwave.rc.generate.request.schema.json` | `xdebug/examples/requests/nwave.rc.generate.basic.json` |
| `value.at` | stable | waveform | waveform | query | 需要在一个或多个精确时间批量读取单信号、list、APB、stream 或 AXI 配置中的值，或显式 clock 采样上下文。 | 需要原始值变化时间线或连续时间范围导出。; 需要未加载的临时多信号布尔表达式求值。 | [{"action": "signal.changes", "when": "需要每次原始值变化。"}, {"action": "expr.eval_at", "when": "需要在同一采样时刻求多信号表达式。"}] | 以 action schema 为准 | 按一个或多个指定时间读取单信号、命名信号列表或接口配置维护的值。 | Read a signal or every value maintained by a named list, APB, stream, or AXI configuration at one or more exact waveform times. | `xdebug/schemas/v1/actions/value.at.request.schema.json` | `xdebug/examples/requests/value.at.basic.json` |
| `verify.conditions` | stable | waveform | waveform | verify | 需要在一个采样时刻验证命名条件集合。 | 需要跨时间窗口的持续性结论或原始表达式求值。 | [{"action": "window.verify", "when": "需要跨时间窗口验证。"}, {"action": "expr.eval_at", "when": "需要一个表达式在一个时刻的值。"}] | conditions, time, clock, signals | 在单个时间验证条件集合。 | Verify multiple conditions at one sampled time. | `xdebug/schemas/v1/actions/verify.conditions.request.schema.json` | `xdebug/examples/requests/verify.conditions.basic.json` |
| `window.verify` | stable | waveform | waveform | verify | 需要在时间窗口内按 clock sample 验证多个条件。 | 只需要单个采样时刻的条件结果或单个信号稳定性。 | [{"action": "verify.conditions", "when": "需要单个时刻的条件验证。"}, {"action": "signal.stability", "when": "需要单信号稳定性检查。"}] | clock, conditions, signals | 按 clock 在窗口内验证条件。 | Verify conditions across a clock-sampled time window. | `xdebug/schemas/v1/actions/window.verify.request.schema.json` | `xdebug/examples/requests/window.verify.basic.json` |
| `stream.config.load` | stable | waveform | waveform | configure | Have one or more raw valid-ready stream definitions that must be registered and statically validated. | Do not use it to list/read an existing definition or analyze observed transfers. | [{"action": "stream.config.list", "when": "List saved stream names."}, {"action": "stream.config.get", "when": "Read one raw saved definition."}, {"action": "stream.query", "when": "Analyze observed stream activity."}] | one of config, one of config_path | 加载 stream 配置。 | Load and validate valid-ready stream configurations. | `xdebug/schemas/v1/actions/stream.config.load.request.schema.json` | `xdebug/examples/requests/stream.config.load.basic.json` |
| `stream.config.list` | stable | waveform | waveform | discover | Need only the names and inventory of saved stream configurations before choosing one. | Do not use it to retrieve one raw definition or resolve signals against a waveform. | [{"action": "stream.config.get", "when": "Read one exact persisted definition."}, {"action": "stream.describe", "when": "Resolve and describe a stream against the active waveform."}] | 以 action schema 为准 | 列出 stream 配置。 | List saved stream configurations. | `xdebug/schemas/v1/actions/stream.config.list.request.schema.json` | `xdebug/examples/requests/stream.config.list.basic.json` |
| `stream.describe` | stable | waveform | waveform | query | Know a saved stream name and need its signals resolved against the active waveform with static and activity metadata. | Do not use it to return the untouched persisted object or analyze dynamic transfers. | [{"action": "stream.config.get", "when": "Read the raw persisted configuration."}, {"action": "stream.query", "when": "Analyze observed transfers, stalls, or packets."}] | stream | 显示 stream 定义和摘要。 | Show a stream definition and its resolved signal metadata. | `xdebug/schemas/v1/actions/stream.describe.request.schema.json` | `xdebug/examples/requests/stream.describe.basic.json` |
| `stream.config.get` | stable | waveform | waveform | query | Read the persisted definition for one exact stream configuration name. | Listing all stream names or resolving the configuration against the active waveform. | [{"action": "stream.describe", "when": "Resolved signals and static validation against the active waveform are required."}] | name | 读取一个已保存的原始 stream 配置，不解析波形元数据。 | Read one saved stream configuration without resolving waveform metadata. | `xdebug/schemas/v1/actions/stream.config.get.request.schema.json` | `xdebug/examples/requests/stream.config.get.basic.json` |
| `stream.validate` | stable | waveform | waveform | verify | Validate stream configuration statically, or reuse an explicit full/range base analysis for dynamic validation. | Do not use it to return transfer evidence or export artifacts. | [{"action": "stream.query", "when": "Return observed stream evidence."}, {"action": "stream.export", "when": "Write stream artifacts."}] | stream | 验证 stream 配置；动态验证可显式选择 full 或 range 基础分析缓存范围。 | Validate stream configuration and observed behavior; dynamic validation supports explicit full or range base-cache scope. | `xdebug/schemas/v1/actions/stream.validate.request.schema.json` | `xdebug/examples/requests/stream.validate.basic.json` |
| `stream.query` | stable | waveform | waveform | query | 接口不是标准 APB/AXI 专用事务，且已加载通用 stream 配置，需要查询 transfer、stall、packet 或任意命名字段。 | 接口是标准 AXI/APB，且需要地址、ID、通道握手或协议 transaction 专用语义。 | [{"action": "event.find", "when": "只需一次性表达式找点。"}, {"action": "apb.query", "when": "查询标准 APB completed transfer。"}, {"action": "axi.query", "when": "查询标准 AXI channel 或 reconstructed transaction。"}] | stream, query | 查询通用 valid-ready transfer、stall 或 packet，并对多个自定义字段执行 exact、range 或 mask 过滤。 | Query generic valid-ready transfers, stalls, and packets with multi-field exact, range, or mask filters. | `xdebug/schemas/v1/actions/stream.query.request.schema.json` | `xdebug/examples/requests/stream.query.basic.json` |
| `stream.export` | stable | waveform | waveform | export | Export stream transfers or packet data while reusing an explicit full/range base analysis. | Do not use it when no artifact/preview export is required or when applying interactive query filters. | [{"action": "stream.query", "when": "Return filtered stream evidence in the response."}] | stream | 从显式 full 或 range 基础分析缓存范围导出 stream 查询结果。 | Export stream transfers or packet data from an explicit full or range base-cache scope. | `xdebug/schemas/v1/actions/stream.export.request.schema.json` | `xdebug/examples/requests/stream.export.basic.json` |

共 73 个当前公开 action。主流程见 [xdebug capability](../capabilities/xdebug.md)。
